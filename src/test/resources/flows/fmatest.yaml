id: example
namespace: io.kestra.fmatest

tasks:
- id: measurements
  type: io.kestra.plugin.fmatest.
  format: "{{uri}}"
- id: query
  type: io.kestra.plugin.fmatest.QueryTask
  format: "{{fetchType}}"
