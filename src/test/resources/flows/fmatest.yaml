id: example
namespace: io.kestra.fmatest

tasks:
- id: date
  type: io.kestra.plugin.fmatest.Example
  format: "{{taskrun.startDate}}"
- id: task-id
  type: io.kestra.plugin.fmatest.Example
  format: "{{task.id}}"
- id: flow-id
  type: io.kestra.plugin.fmatest.Example
  format: "{{outputs['task-id'].child.value}}"
